import React, { ReactElement } from 'react';
import { Typography } from '@material-ui/core';

import { Layout, Seo, {{pascalCase name}}Map } from '../components';
import {
    StyledPageContainer,
    StyledMapContainer,
} from '../pages-lib/{{kebabCase name}}-map/{{kebabCase name}}-map.styled';

export default function {{pascalCase name}}Page(): ReactElement {
    return (
        <Layout>
            <StyledPageContainer>
                <Seo title="{{pascalCase name}}" />
                <Typography variant="h3" component="h2">
                    {{pascalCase name}} Map
                </Typography>
                <StyledMapContainer>
                    <{{pascalCase name}}Map />
                </StyledMapContainer>
                <Typography variant="body1" gutterBottom>
                    This is just a {{pascalCase name}} map.
                </Typography>
            </StyledPageContainer>
        </Layout>
    );
}
